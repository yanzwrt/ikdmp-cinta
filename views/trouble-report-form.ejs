<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Laporan Gangguan - <%= companyHeader %></title>
    <link rel="manifest" href="/manifest.webmanifest">
    <meta name="theme-color" content="#0B1437">
    <link rel="icon" href="/favicon.ico" sizes="any">
    <link rel="apple-touch-icon" href="/img/icons/icon-192.png">
    <link rel="stylesheet" href="/css/pwa.css">
    <link rel="stylesheet" href="/css/customer-responsive.css">
    <script defer src="/js/pwa-install.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <style>
        :root {
            --primary-color: #4361ee;
            --secondary-color: #3f37c9;
            --accent-color: #4895ef;
            --success-color: #4cc9f0;
            --warning-color: #f72585;
            --danger-color: #e63946;
            --light-color: #f8f9fa;
            --dark-color: #212529;
            --card-border-radius: 16px;
            --btn-border-radius: 50px;
            --transition-speed: 0.3s;
        }
        
        body {
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            color: var(--dark-color);
            line-height: 1.6;
        }
        
        .navbar {
            background: rgba(255, 255, 255, 0.95) !important;
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(0, 0, 0, 0.05);
            box-shadow: 0 2px 15px rgba(0, 0, 0, 0.05);
        }
        
        .navbar-brand {
            font-weight: 700;
            color: var(--primary-color) !important;
            letter-spacing: -0.5px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .main-container {
            padding: 30px 0;
            min-height: calc(100vh - 80px);
        }
        
        .form-card {
            background: white;
            border-radius: var(--card-border-radius);
            box-shadow: 0 10px 30px rgba(0,0,0,0.08);
            overflow: hidden;
            margin-bottom: 30px;
        }
        
        .form-header {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            padding: 30px;
            text-align: center;
        }
        
        .form-header h2 {
            margin: 0;
            font-weight: 700;
            font-size: 1.8rem;
        }
        
        .form-header p {
            margin: 10px 0 0 0;
            opacity: 0.9;
        }
        
        .form-body {
            padding: 40px;
        }
        
        .form-label {
            font-weight: 600;
            color: var(--dark-color);
            margin-bottom: 8px;
        }
        
        .form-control, .form-select {
            border-radius: 12px;
            padding: 15px 20px;
            border: 2px solid #e9ecef;
            font-size: 16px;
            transition: all var(--transition-speed) ease;
        }
        
        .form-control:focus, .form-select:focus {
            box-shadow: 0 0 0 3px rgba(67, 97, 238, 0.15);
            border-color: var(--primary-color);
        }
        
        textarea.form-control {
            min-height: 120px;
            resize: vertical;
        }
        
        .btn {
            padding: 15px 30px;
            border-radius: var(--btn-border-radius);
            font-weight: 600;
            font-size: 16px;
            transition: all var(--transition-speed) ease;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            text-decoration: none;
        }
        
        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.15);
        }
        
        .btn-primary {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            border: none;
        }
        
        .btn-outline-secondary {
            border: 2px solid var(--primary-color);
            color: var(--primary-color);
            background: transparent;
        }
        
        .btn-outline-secondary:hover {
            background: var(--primary-color);
            color: white;
        }
        
        .alert {
            border-radius: 12px;
            border: none;
            padding: 20px;
        }
        
        .char-counter {
            font-size: 14px;
            color: #6c757d;
            text-align: right;
            margin-top: 5px;
        }
        
        .char-counter.warning {
            color: var(--warning-color);
            font-weight: 600;
        }
        
        .quick-actions {
            background: #f8f9fa;
            border-radius: 12px;
            padding: 20px;
            margin-top: 20px;
        }
        
        .quick-actions h6 {
            color: var(--primary-color);
            font-weight: 700;
            margin-bottom: 15px;
        }
        
        .category-badge {
            display: inline-block;
            padding: 8px 16px;
            background: var(--light-color);
            border: 2px solid #e9ecef;
            border-radius: 25px;
            margin: 5px;
            cursor: pointer;
            transition: all var(--transition-speed) ease;
            font-size: 14px;
        }
        
        .category-badge:hover {
            background: var(--primary-color);
            color: white;
            border-color: var(--primary-color);
        }
        
        @media (max-width: 768px) {
            .form-body {
                padding: 20px;
            }
            
            .form-header {
                padding: 20px;
            }
            
            .form-header h2 {
                font-size: 1.5rem;
            }
            
            .btn {
                padding: 12px 24px;
                font-size: 14px;
            }
        }
    </style>
</head>
<body>
    <!-- Navbar atas dihilangkan untuk tampilan mobile minimalis -->

    <div class="main-container">
        <div class="container">
            <%- include('partials/customer-mobile-profile-header') %>
            <div class="row justify-content-center">
                <div class="col-lg-8">
                    <!-- Form Card -->
                    <div class="form-card">
                        <div class="form-header">
                            <h2><i class="bi bi-exclamation-triangle me-2"></i>Laporan Gangguan</h2>
                            <p>Laporkan masalah teknis yang Anda alami, tim teknisi kami akan segera membantu</p>
                        </div>
                        
                        <div class="form-body">
                            <form id="troubleReportForm">
                                <div class="row">
                                    <div class="col-md-6 mb-3">
                                        <label for="name" class="form-label">
                                            <i class="bi bi-person me-1"></i>Nama Lengkap *
                                        </label>
                                        <input type="text" class="form-control" id="name" value="<%= customerName %>" required>
                                    </div>
                                    
                                    <div class="col-md-6 mb-3">
                                        <label for="phone" class="form-label">
                                            <i class="bi bi-telephone me-1"></i>Nomor Telepon
                                        </label>
                                        <input type="text" class="form-control" id="phone" value="<%= phone %>" readonly>
                                    </div>
                                </div>

                                <div class="mb-3">
                                    <label for="location" class="form-label">
                                        <i class="bi bi-geo-alt me-1"></i>Lokasi/Alamat
                                    </label>
                                    <input type="text" class="form-control" id="location" value="<%= location %>" placeholder="Masukkan alamat lengkap Anda">
                                </div>

                                <div class="mb-3">
                                    <label for="category" class="form-label">
                                        <i class="bi bi-tags me-1"></i>Kategori Gangguan *
                                    </label>
                                    <select class="form-select" id="category" required>
                                        <option value="">Pilih kategori gangguan...</option>
                                        <% categories.forEach(function(cat) { %>
                                        <option value="<%= cat %>"><%= cat %></option>
                                        <% }); %>
                                    </select>
                                </div>

                                <div class="quick-actions">
                                    <h6><i class="bi bi-lightning me-1"></i>Kategori Umum:</h6>
                                    <% categories.forEach(function(cat) { %>
                                    <span class="category-badge" onclick="selectCategory('<%= cat %>')">
                                        <%= cat %>
                                    </span>
                                    <% }); %>
                                </div>

                                <div class="mb-4">
                                    <label for="description" class="form-label">
                                        <i class="bi bi-chat-text me-1"></i>Deskripsi Masalah *
                                    </label>
                                    <textarea class="form-control" id="description" rows="5" 
                                              placeholder="Jelaskan masalah yang Anda alami secara detail. Semakin jelas deskripsi, semakin cepat kami dapat membantu Anda."
                                              maxlength="500" required></textarea>
                                    <div class="char-counter">
                                        <span id="charCount">0</span>/500 karakter
                                    </div>
                                </div>

                                <div class="mb-4">
                                    <label for="device_photo" class="form-label">
                                        <i class="bi bi-camera me-1"></i>Foto Modem / ONT
                                    </label>
                                    <input type="file" class="form-control" id="device_photo" accept="image/*" capture="environment">
                                    <div class="form-text">Opsional. Bisa pilih file atau langsung buka kamera belakang HP untuk foto modem/ONT.</div>
                                    <div id="devicePhotoPreviewWrap" class="mt-3 d-none">
                                        <img id="devicePhotoPreview" alt="Preview foto perangkat" class="img-fluid rounded-4 border" style="max-height: 220px; object-fit: cover;">
                                    </div>
                                </div>

                                <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                                    <a href="/customer/dashboard" class="btn btn-outline-secondary me-md-2">
                                        <i class="bi bi-arrow-left"></i> Kembali
                                    </a>
                                    <button type="submit" class="btn btn-primary">
                                        <i class="bi bi-send"></i> Kirim Laporan
                                    </button>
                                </div>
                            </form>
                        </div>
                    </div>

                    <!-- Riwayat Laporan Gangguan -->
                    <div class="form-card">
                        <div class="form-header">
                            <h2><i class="bi bi-clock-history me-2"></i>Riwayat Laporan Anda</h2>
                            <p>Lihat status penanganan laporan yang pernah Anda kirim</p>
                        </div>
                        <div class="form-body">
                            <% if (typeof previousReports !== 'undefined' && previousReports && previousReports.length > 0) { %>
                                <div class="table-responsive d-none d-md-block">
                                    <table class="table table-hover align-middle mb-0">
                                        <thead>
                                            <tr>
                                                <th>ID</th>
                                                <th>Tanggal</th>
                                                <th>Kategori</th>
                                                <th>Lokasi</th>
                                                <th>Status</th>
                                                <th>Catatan Terakhir</th>
                                                <th>Aksi</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <% previousReports.slice().reverse().forEach(function(rep){ %>
                                                <tr>
                                                    <td>#<%= rep.id %></td>
                                                    <td>
                                                        <% if (rep.createdAt) { %>
                                                            <%= new Date(rep.createdAt).toLocaleDateString('id-ID') %>
                                                            <br><small class="text-muted">
                                                                <%= new Date(rep.createdAt).toLocaleTimeString('id-ID') %>
                                                            </small>
                                                        <% } else if (rep.created_at) { %>
                                                            <%= new Date(rep.created_at).toLocaleDateString('id-ID') %>
                                                            <br><small class="text-muted">
                                                                <%= new Date(rep.created_at).toLocaleTimeString('id-ID') %>
                                                            </small>
                                                        <% } else { %>
                                                            -
                                                        <% } %>
                                                    </td>
                                                    <td><%= rep.category || '-' %></td>
                                                    <td>
                                                        <% if (rep.location && rep.location.length > 50) { %>
                                                            <span title="<%= rep.location %>">
                                                                <%= rep.location.substring(0, 50) %>...
                                                            </span>
                                                        <% } else { %>
                                                            <%= rep.location || '-' %>
                                                        <% } %>
                                                    </td>
                                                    <td>
                                                        <% 
                                                        let statusText = 'Unknown';
                                                        let statusClass = 'bg-secondary';
                                                        if (rep.status === 'open') {
                                                            statusText = 'Dibuka';
                                                            statusClass = 'bg-danger';
                                                        } else if (rep.status === 'in_progress') {
                                                            statusText = 'Sedang Ditangani';
                                                            statusClass = 'bg-warning text-dark';
                                                        } else if (rep.status === 'resolved') {
                                                            statusText = 'Terselesaikan';
                                                            statusClass = 'bg-success';
                                                        } else if (rep.status === 'closed') {
                                                            statusText = 'Ditutup';
                                                            statusClass = 'bg-secondary';
                                                        }
                                                        %>
                                                        <span class="badge <%= statusClass %>">
                                                            <%= statusText %>
                                                        </span>
                                                    </td>
                                                    <td style="max-width: 320px;">
                                                        <% 
                                                        let lastNote = '-';
                                                        if (rep.notes && Array.isArray(rep.notes) && rep.notes.length > 0) {
                                                            const lastNoteObj = rep.notes[rep.notes.length - 1];
                                                            if (typeof lastNoteObj === 'object' && lastNoteObj.content) {
                                                                lastNote = lastNoteObj.content;
                                                            } else if (typeof lastNoteObj === 'string') {
                                                                lastNote = lastNoteObj;
                                                            }
                                                        }
                                                        if (lastNote.length > 50) {
                                                            lastNote = lastNote.substring(0, 50) + '...';
                                                        }
                                                        %>
                                                        <span title="<%= lastNote %>"><%= lastNote %></span>
                                                    </td>
                                                    <td>
                                                        <a href="/customer/trouble/detail/<%= rep.id %>" class="btn btn-sm btn-outline-primary">Detail</a>
                                                    </td>
                                                </tr>
                                            <% }) %>
                                        </tbody>
                                    </table>
                                </div>

                                <!-- Mobile cards -->
                                <div class="d-block d-md-none">
                                    <div class="vstack gap-3">
                                        <% previousReports.slice().reverse().forEach(function(rep){ %>
                                            <div class="card shadow-sm">
                                                <div class="card-body">
                                                    <div class="d-flex justify-content-between mb-2">
                                                        <div>
                                                            <div class="small text-muted">ID Laporan</div>
                                                            <div class="fw-bold">#<%= rep.id %></div>
                                                        </div>
                                                        <div class="text-end small text-muted">
                                                            <% if (rep.createdAt) { %>
                                                                <%= new Date(rep.createdAt).toLocaleDateString('id-ID') %>
                                                                <br><%= new Date(rep.createdAt).toLocaleTimeString('id-ID') %>
                                                            <% } else if (rep.created_at) { %>
                                                                <%= new Date(rep.created_at).toLocaleDateString('id-ID') %>
                                                                <br><%= new Date(rep.created_at).toLocaleTimeString('id-ID') %>
                                                            <% } else { %>
                                                                -
                                                            <% } %>
                                                        </div>
                                                    </div>
                                                    <div class="mb-2">
                                                        <span class="badge bg-info"><%= rep.category || '-' %></span>
                                                        <% 
                                                        let mobileStatusText = 'Unknown';
                                                        let mobileStatusClass = 'bg-secondary';
                                                        if (rep.status === 'open') {
                                                            mobileStatusText = 'Dibuka';
                                                            mobileStatusClass = 'bg-danger';
                                                        } else if (rep.status === 'in_progress') {
                                                            mobileStatusText = 'Sedang Ditangani';
                                                            mobileStatusClass = 'bg-warning text-dark';
                                                        } else if (rep.status === 'resolved') {
                                                            mobileStatusText = 'Terselesaikan';
                                                            mobileStatusClass = 'bg-success';
                                                        } else if (rep.status === 'closed') {
                                                            mobileStatusText = 'Ditutup';
                                                            mobileStatusClass = 'bg-secondary';
                                                        }
                                                        %>
                                                        <span class="badge <%= mobileStatusClass %> ms-2">
                                                            <%= mobileStatusText %>
                                                        </span>
                                                    </div>
                                                    <div class="small text-muted mb-1">
                                                        Lokasi: 
                                                        <% if (rep.location && rep.location.length > 40) { %>
                                                            <span title="<%= rep.location %>">
                                                                <%= rep.location.substring(0, 40) %>...
                                                            </span>
                                                        <% } else { %>
                                                            <%= rep.location || '-' %>
                                                        <% } %>
                                                    </div>
                                                    <div class="small mb-2" style="max-width: 100%;">
                                                        <strong>Catatan:</strong><br>
                                                        <% 
                                                        let mobileLastNote = '-';
                                                        if (rep.notes && Array.isArray(rep.notes) && rep.notes.length > 0) {
                                                            const lastNoteObj = rep.notes[rep.notes.length - 1];
                                                            if (typeof lastNoteObj === 'object' && lastNoteObj.content) {
                                                                mobileLastNote = lastNoteObj.content;
                                                            } else if (typeof lastNoteObj === 'string') {
                                                                mobileLastNote = lastNoteObj;
                                                            }
                                                        }
                                                        if (mobileLastNote.length > 80) {
                                                            mobileLastNote = mobileLastNote.substring(0, 80) + '...';
                                                        }
                                                        %>
                                                        <em><%= mobileLastNote %></em>
                                                    </div>
                                                    <a href="/customer/trouble/detail/<%= rep.id %>" class="btn btn-primary w-100 mt-2">Detail</a>
                                                </div>
                                            </div>
                                        <% }) %>
                                    </div>
                                </div>

                                <div class="text-center mt-3">
                                    <a href="/customer/trouble/list" class="btn btn-outline-primary">
                                        <i class="bi bi-arrow-right me-1"></i>Lihat Semua Laporan (<%= previousReports.length %>)
                                    </a>
                                </div>
                            <% } else { %>
                                <div class="alert alert-info mb-0">
                                    <i class="bi bi-info-circle me-2"></i>
                                    Belum ada riwayat laporan. Setelah mengirim laporan, riwayat akan muncul di sini.
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Toast Notifications -->
    <div class="toast-container position-fixed top-0 end-0 p-3" style="z-index: 1055;">
        <div id="successToast" class="toast" role="alert">
            <div class="toast-header bg-success text-white">
                <i class="bi bi-check-circle-fill me-2"></i>
                <strong class="me-auto">Berhasil!</strong>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="toast"></button>
            </div>
            <div class="toast-body" id="successMessage"></div>
        </div>
        
        <div id="errorToast" class="toast" role="alert">
            <div class="toast-header bg-danger text-white">
                <i class="bi bi-exclamation-triangle-fill me-2"></i>
                <strong class="me-auto">Error!</strong>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="toast"></button>
            </div>
            <div class="toast-body" id="errorMessage"></div>
        </div>
    </div>

    <!-- Success Modal -->
    <div class="modal fade" id="successModal" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header bg-success text-white">
                    <h5 class="modal-title">
                        <i class="bi bi-check-circle-fill me-2"></i>Laporan Berhasil Dibuat
                    </h5>
                </div>
                <div class="modal-body" id="successModalBody"></div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-primary" data-bs-dismiss="modal" id="modalOkButton">
                        <i class="bi bi-check2"></i> OK
                    </button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/customer-mobile.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const form = document.getElementById('troubleReportForm');
            const submitButton = form.querySelector('button[type="submit"]');
            const textarea = document.getElementById('description');
            const charCount = document.getElementById('charCount');
            const charCounter = document.querySelector('.char-counter');
            const nameInput = document.getElementById('name');
            const phoneInput = document.getElementById('phone');
            const locationInput = document.getElementById('location');
            const devicePhotoInput = document.getElementById('device_photo');
            const devicePhotoPreview = document.getElementById('devicePhotoPreview');
            const devicePhotoPreviewWrap = document.getElementById('devicePhotoPreviewWrap');

            if (devicePhotoInput) {
                devicePhotoInput.addEventListener('change', function() {
                    const file = this.files && this.files[0];
                    if (!file) {
                        devicePhotoPreviewWrap.classList.add('d-none');
                        devicePhotoPreview.src = '';
                        return;
                    }
                    const reader = new FileReader();
                    reader.onload = function(event) {
                        devicePhotoPreview.src = event.target.result;
                        devicePhotoPreviewWrap.classList.remove('d-none');
                    };
                    reader.readAsDataURL(file);
                });
            }

            // Auto-fill dari data profile billing jika field masih kosong
            fetch('/customer/billing/api/profile', { headers: { 'Accept': 'application/json' } })
                .then(r => r.ok ? r.json() : null)
                .then(customer => {
                    if (!customer) return;
                    if (nameInput && !nameInput.value.trim()) {
                        nameInput.value = (customer.name || customer.username || customer.pppoe_username || '').trim();
                    }
                    if (locationInput && !locationInput.value.trim()) {
                        locationInput.value = (customer.address || '').trim();
                    }
                    if (phoneInput && !phoneInput.value.trim()) {
                        phoneInput.value = (customer.phone || '').trim();
                    }
                })
                .catch(() => {
                    // Silent fallback: tetap gunakan nilai render server
                });
            
            // Toast notifications
            const successToast = new bootstrap.Toast(document.getElementById('successToast'));
            const errorToast = new bootstrap.Toast(document.getElementById('errorToast'));
            const successModal = new bootstrap.Modal(document.getElementById('successModal'));
            
            // Character counter
            textarea.addEventListener('input', function() {
                const length = this.value.length;
                charCount.textContent = length;
                
                if (length > 450) {
                    charCounter.classList.add('warning');
                } else {
                    charCounter.classList.remove('warning');
                }
                
                // Auto resize
                this.style.height = 'auto';
                this.style.height = Math.max(120, this.scrollHeight) + 'px';
            });
            
            // Form submission
            form.addEventListener('submit', function(e) {
                e.preventDefault();
                
                const formData = new FormData();
                formData.append('name', document.getElementById('name').value.trim());
                formData.append('location', document.getElementById('location').value.trim());
                formData.append('category', document.getElementById('category').value);
                formData.append('description', textarea.value.trim());
                if (devicePhotoInput && devicePhotoInput.files && devicePhotoInput.files[0]) {
                    formData.append('device_photo', devicePhotoInput.files[0]);
                }
                
                // Validation
                if (!formData.get('category') || !formData.get('description')) {
                    document.getElementById('errorMessage').textContent = 'Kategori dan deskripsi masalah wajib diisi';
                    errorToast.show();
                    return;
                }
                
                // Loading state
                const originalText = submitButton.innerHTML;
                submitButton.disabled = true;
                submitButton.innerHTML = '<span class="spinner-border spinner-border-sm me-2"></span>Mengirim...';
                
                // Submit to server
                fetch('/customer/trouble/report', {
                    method: 'POST',
                    body: formData
                })
                .then(response => {
                    if (!response.ok) {
                        throw new Error(`HTTP ${response.status}: ${response.statusText}`);
                    }
                    return response.json();
                })
                .then(data => {
                    if (data.success) {
                        // Show success toast
                        document.getElementById('successMessage').textContent = 'Laporan gangguan berhasil dibuat';
                        successToast.show();
                        
                        // Reset form
                        form.reset();
                        document.getElementById('name').value = '<%= customerName %>';
                        document.getElementById('phone').value = '<%= phone %>';
                        document.getElementById('location').value = '<%= location %>';
                        charCount.textContent = '0';
                        charCounter.classList.remove('warning');
                        textarea.style.height = '120px';
                        devicePhotoPreviewWrap.classList.add('d-none');
                        devicePhotoPreview.src = '';
                        
                        // Show success modal after 1.5s
                        setTimeout(() => {
                            document.getElementById('successModalBody').innerHTML = `
                                <div class="alert alert-success">
                                    <h6><i class="bi bi-check-circle me-2"></i>ID Laporan: <strong>${data.reportId}</strong></h6>
                                    <p class="mb-2">Tim teknisi akan segera menindaklanjuti laporan Anda.</p>
                                    <small class="text-muted">
                                        <i class="bi bi-whatsapp me-1"></i>
                                        Anda akan menerima notifikasi WhatsApp untuk update status.
                                    </small>
                                </div>
                            `;
                            
                            document.getElementById('modalOkButton').onclick = function() {
                                window.location.href = '/customer/dashboard';
                            };
                            
                            successModal.show();
                        }, 1500);
                        
                    } else {
                        throw new Error(data.message || 'Gagal membuat laporan gangguan');
                    }
                })
                .catch(error => {
                    let errorMessage = 'Terjadi kesalahan saat mengirim laporan. Silakan coba lagi.';
                    
                    if (error.message.includes('NetworkError') || error.message.includes('Failed to fetch')) {
                        errorMessage = 'Tidak dapat terhubung ke server. Periksa koneksi internet Anda.';
                    } else if (error.message.includes('HTTP 401')) {
                        errorMessage = 'Session Anda telah berakhir. Silakan login ulang.';
                        setTimeout(() => { window.location.href = '/customer/login'; }, 2000);
                    } else if (error.message.includes('HTTP 403')) {
                        errorMessage = 'Anda tidak memiliki akses untuk membuat laporan gangguan.';
                    } else if (error.message.includes('HTTP 500')) {
                        errorMessage = 'Terjadi kesalahan pada server. Silakan coba lagi nanti.';
                    } else {
                        errorMessage = error.message;
                    }
                    
                    document.getElementById('errorMessage').textContent = errorMessage;
                    errorToast.show();
                })
                .finally(() => {
                    // Reset button
                    submitButton.disabled = false;
                    submitButton.innerHTML = originalText;
                });
            });
        });
        
        // Quick category selection
        function selectCategory(category) {
            document.getElementById('category').value = category;
            document.getElementById('description').focus();
        }
        </script>

    <div class="install-banner">
        <button class="btn" data-install-app style="display:none">Install Aplikasi</button>
    </div>

    <!-- Bottom nav (mobile) 5 menu -->
    <nav class="bottom-nav d-md-none">
    <a class="nav-item" href="/customer/billing/dashboard">
        <i class="bi bi-columns-gap"></i>
        <span>Billing</span>
    </a>
    <a class="nav-item active" href="/customer/trouble/report">
        <i class="bi bi-life-preserver"></i>
        <span>Laporan</span>
    </a>
    <a class="nav-item home-button" href="/customer/dashboard">
        <i class="bi bi-house-heart-fill"></i>
        <span>Home</span>
    </a>
    <a class="nav-item" href="/customer/billing/invoices">
        <i class="bi bi-receipt"></i>
        <span>Invoice</span>
    </a>
    <a class="nav-item" href="/customer/billing/profile">
        <i class="bi bi-person-vcard"></i>
        <span>Profile</span>
    </a>
</nav>

    <script>
      // Tandai menu aktif berdasarkan path
      (function(){
        var path = location.pathname;
        var items = document.querySelectorAll('.bottom-nav .nav-item');
        function mark(index){ if(items[index]) items[index].classList.add('active'); }
        if(path.startsWith('/customer/billing/dashboard')) mark(0);
        else if(path.startsWith('/customer/trouble')) mark(1);
        else if(path.startsWith('/customer/dashboard')) mark(2);
        else if(path.startsWith('/customer/billing/invoices')) mark(3);
        else if(path.startsWith('/customer/billing/profile')) mark(4);
      })();
    </script>
    <%- include('partials/customer-trouble-notification-script') %>

</body>
</html>

